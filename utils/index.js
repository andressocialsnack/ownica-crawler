const flatten = require('./flatten')
const isDirectory = require("./isDirectory");
const isError = require("./isError");
const withError = require("./withError");

module.exports = {
  flatten,
  isDirectory,
  isError,
  withError,
};
